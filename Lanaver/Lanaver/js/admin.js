const loginPanel=document.getElementById('loginPanel'),dashboard=document.getElementById('dashboard');
function loadDashboard(){const tx=JSON.parse(localStorage.getItem('lanaver_transactions')||'[]');const money=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n);document.getElementById('statTransactions').textContent=tx.length;document.getElementById('statRevenue').textContent=money(tx.reduce((s,t)=>s+t.total,0));document.getElementById('statItems').textContent=tx.reduce((s,t)=>s+t.items.reduce((a,i)=>a+i.qty,0),0);document.getElementById('transactionTable').innerHTML=tx.map(t=>`<tr><td>${t.time}</td><td>${t.customer}</td><td>${t.method.toUpperCase()}</td><td>${t.items.reduce((s,i)=>s+i.qty,0)}</td><td>${money(t.total)}</td></tr>`).join('')||'<tr><td colspan="5">Belum ada transaksi.</td></tr>'}
document.getElementById('loginBtn')?.addEventListener('click',()=>{if(document.getElementById('username').value==='Admin'&&document.getElementById('password').value==='Admin123'){sessionStorage.setItem('lanaver_admin','1');loginPanel.classList.add('hidden');dashboard.classList.remove('hidden');loadDashboard()}else document.getElementById('loginError').textContent='Username atau password salah.'});document.getElementById('logoutBtn')?.addEventListener('click',()=>{sessionStorage.removeItem('lanaver_admin');dashboard.classList.add('hidden');loginPanel.classList.remove('hidden')});if(sessionStorage.getItem('lanaver_admin')==='1'){loginPanel.classList.add('hidden');dashboard.classList.remove('hidden');loadDashboard()}
